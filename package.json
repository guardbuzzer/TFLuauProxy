{
  "name": "tfluau-proxy",
  "version": "1.0.0",
  "description": "A proxy to allow TFLuau to authenticate with TfL APIs",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler dev",
    "publish": "wrangler deploy",
    "format": "prettier --write '**/*.{js,css,json,md}'",
    "format:check": "prettier --check '**/*.{js,css,json,md}'"
  },
  "author": "guardbuzzer",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^3.3.3",
    "wrangler": "^4.28.0"
  }
}
